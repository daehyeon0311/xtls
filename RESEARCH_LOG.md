# 연구 로그

Ba₂FeSi₂O₇ / Sr₂FeSi₂O₇ 전하이동 클러스터 모델 작업 기록.
새 항목은 **파일 아래쪽에 누적**한다.

---

## 배경

**기반 논문**
J.-H. Choi, D.-H. Bahng, T.-H. Jang, J.-H. Park,
*X-ray linear dichroism study on S = 2 antiferromagnets Ba₂FeSi₂O₇ and Sr₂FeSi₂O₇:
determination of single-ion magnetic anisotropy energies*,
J. Korean Phys. Soc. **88**, 933 (2026).

- Fe L₂,₃ XAS/XLD + full multiplet 클러스터 모델(XTLS)
- 단일이온 자기이방성 **D_BFSO = 1.45 meV**, **D_SFSO = 1.22 meV**
- 중성자 산란 실측값 D_BFSO = 1.42 meV (Do et al.)와 부합
- 메커니즘: D ~ λ²/ΔE, ΔE = E(yz,zx) − E(3z²−r²)
- 왜곡각 Δθ가 커지면 ΔE 감소 → D 증가, Δθ ≈ 10°에서 임계값 D_c = 1.65 meV 도달 예측
- 파라미터: Δ = 5.5, U_dd = 4.5, U_pd = 5.75 eV, Slater 82.5%

**목표**
실험 없이 계산만으로 후속 논문. 우선순위는 다음 순서로 합의:

| | 단계 | DFT 필요 |
|---|---|---|
| 1 | D(Δθ) 재현·확장 (Fig. 3) | ✕ |
| 2 | 4차 이방성·터널링 분열 정량화 | ✕ |
| 3 | Poisson 비 가정 감도 분석 | ✕ |
| 4 | A₂MX₂O₇ 계열 스크리닝 | ✕ |
| 5 | XPS·XMCD·RIXS 예측 | ✕ |
| 6 | 구조 이완 + J(Δθ) | ✓ |

**논문이 남긴 빈틈** (후속의 출발점)

1. **J(Δθ)를 계산하지 않음.** 임계 조건은 D/J = α_c인데 J는 "왜곡에 따른 변화가 미미할 것"이라는 정성적 상쇄 논증뿐. 근거는 T_N이 5.2 K와 5.0 K로 비슷하다는 간접 증거. Δθ ≈ 10° 예측이 J = 0.26 meV 고정 가정 위에 서 있음.
2. **Poisson 비 ν = 0.492 고정.** 두 실험 구조에서 뽑은 값을 4°~12° 전 구간에 외삽.
3. **Δθ ≈ 10°를 어떻게 만들지 미제시.** "strain control" 참고문헌만.

---

## 코드 기반 정비 (선행 작업)

후속 연구용 계산 도구를 먼저 정리했다. 저장소: `xtls`(GitHub), 로컬 `C:\Users\DH\Documents\XAS`.

### 정리 내역

| 커밋 | 내용 |
|---|---|
| `bc5644f` | 저장소 구조 정리, 문서·패키징 |
| `f730f33` | 다중체 커널 벡터화, 죽은 코드 제거 (59초 → 32초) |
| `39ca43a` | XPS 계산 추가 |
| `5f159e7` | XAS/XPS 인터페이스 통합, `run.py` 진입점 |
| `a9608cb` | Streamlit GUI (계산 포함 버전) |
| `a5ca3a2` | GUI를 입력 카드 생성기로 축소 |

### 발견하고 고친 문제들

#### ① 배치 에너지 공식이 XPS에 틀림

`sector_energy`가 `core_holes`로 분기하면서 "코어 홀이 있으면 XAS 종상태"(d 전자 오프셋 +1)를 암묵 가정.
XPS 종상태는 코어 홀이 있으면서 오프셋 0이라 **U_dd만큼 어긋남** (NiO에서 7.3 eV).

XTLS 매뉴얼 p.9의 배치 중심 에너지식으로 일반화:

```
E(h) = h·Δ + U_dd · h · (2δ_d + h − 1)/2 − c · h · U_dc
```

| 섹터 | δ_d | c | h=1 에너지 |
|---|---|---|---|
| 초기 3dⁿ | 0 | 0 | Δ |
| XAS 종 2p⁵3dⁿ⁺¹ | +1 | 1 | Δ + U_dd − U_dc |
| XPS 종 2p⁵3dⁿ | 0 | 1 | Δ − U_dc |
| valence XPS 3dⁿ⁻¹ | −1 | 0 | Δ − U_dd |

XAS 경로에 `d_electron_offset=1`을 명시. 결과 불변 확인 (배치 에너지 바이트 동일, 스펙트럼 1e-12).

#### ② `eigsh`가 재현적이지 않음

ARPACK이 랜덤 시작 벡터를 쓴다. 같은 행렬을 연속으로 넣어도 축퇴 부공간의 기저가 달라짐.
직접 확인: 두 번 호출 시 고유벡터 max 차이 **0.53**.

이전에 "eigsh는 결정적"이라고 판단했던 것은 **틀렸다** — 그때 diff가 0으로 나온 건 우연.

`lowest_eigenpairs`에서 고정 시드 시작 벡터를 지정해 해결. 두 번 실행이 비트 단위로 동일해짐.

> **주의**: 이건 선택을 *고정*할 뿐이다. 축퇴 부공간 안에서 어느 기저도 더 옳지 않으므로,
> 부공간 불변량이 아닌 값(개별 상태 Sz·Lz, 스핀 분해 채널, 정방정계 아닌 경우의 x/y 편광)은
> 여전히 물리적으로 유일하지 않다. 합성량(iso, ld, total)만 의미가 있다.

#### ③ GUI를 과하게 만듦

슬라이더 조작마다 스펙트럼을 전체 재계산해서 느렸다 (1.7초/조작).
원래 요구는 "파라미터 설정 → 입력 카드 생성"이었으므로 계산을 들어냄.
첫 렌더 4.6초 → 0.97초, 필드 변경 1.7초 → **0.039초**.

계산 없이 즉시 보여줄 수 있는 것은 남김: 기저 크기·예상 시간, 배치 에너지,
결정장 준위, 해당 배치의 Slater 적분.

### 검증 자산

- **NiO 2p XPS**: XTLS 매뉴얼 pp.20–21 예제 재현. 2p₃/₂ 주선–satellite 6.3 eV(실험 ~7),
  스핀궤도 분리 17.5 eV(실험 ~17.3), 종상태 3d⁸ 6% / 3d⁹L̲ 57% / 3d¹⁰L̲² 37%
- **합 규칙**: 코어준위 XPS 총 세기 = 6.000 (2p⁶ 점유수와 정확히 일치)
- **Appendix-A 표**: 매뉴얼 인쇄값과 소수점 셋째 자리까지 일치
- **교차 검증**: 같은 입력의 XAS와 XPS가 동일한 초기 바닥상태 에너지 (−12.519260649)

---

## 1단계: D(Δθ) 재현 — 진행 중

도구: `run_anisotropy.py`. 초기 해밀토니안만 대각화하므로 기하당 1초 미만.

### D 추출 방법

S=2 다중항 5개 준위를 `E(Sz) = D·Sz² + A·Sz⁴`로 피팅.

```
D = (16·E₁ − E₂)/12,    A = (E₂ − 4·E₁)/12
```
E₁ = Sz=±1 이중항 중심, E₂ = Sz=±2 이중항 중심.

### 첫 결과 (기존 코드, 부호 기반 정규화)

BFSO, Δθ = 7.8353°:
```
Sz = 0     0.0000 meV
Sz = ±1    1.4625 meV
Sz = ±2    5.8646 / 5.8792 meV
→ D = 1.4607 meV,  A = +0.00182 meV,  터널링 분열 = 0.01457 meV
```

**논문 D = 1.45 meV와 0.9% 차이. 사실상 재현.**
SFSO는 D = 1.1717 meV (논문 1.22).
D_c = 1.65 meV 도달 지점 Δθ = 9.634° (논문 "≈ 10°").

다음 상태는 338 meV 위에 있어 최저 5개가 깔끔한 S=2 다중항.

### 새로 얻은 것 — 논문에 없는 항

- **4차 이방성 A**: BFSO +0.0018 meV, SFSO −0.0010 meV. D의 0.1% 수준이지만 부호가 바뀜.
- **Sz = ±2 이중항의 터널링 분열**: BFSO 0.0146 meV, SFSO 0.0087 meV.
  논문에 언급 없음. 4차 이방성에 의한 분열로 보이며, 자화 양자터널링과 관련된 양.

---

## 문제 ④: 결정장 정규화 불연속 — **미해결, 판단 필요**

### 증상

Δθ 스캔에서 D가 비단조:
```
Δθ = 4.00°  D = 1.1211 meV
Δθ = 4.50°  D = 1.2690      ← 튐
Δθ = 5.00°  D = 1.1852      ← 내려감
Δθ = 5.50°  D = 1.2333
```
6번째 준위도 346 → 270 → 377 meV로 요동.

### 원인

고유값은 정확했다 (eigsh k=8/16/30과 조밀 대각화가 소수점 6자리까지 일치).
문제는 **해밀토니안 구축이 각도에 불연속**이라는 것.

`geometry.py`의 결정장 정규화가 준위 **부호**로 그룹을 나눴다:

```python
positive_count = int(np.sum(e_cry > 0))
e_plus  = np.sum(e_cry[5-positive_count:])
e_minus = np.sum(e_cry[:5-positive_count])
dq = e_plus/positive_count - e_minus/(5-positive_count)
norm = ten_dq / dq
```

왜곡에 따라 `yz`·`zx`와 `x²−y²` 준위가 0을 교차하면서:
```
Δθ 4.25° → #positive = 3
Δθ 4.50° → #positive = 1     ← 점프
Δθ 4.75° → #positive = 2
```
그룹 구성이 바뀌고 정규화 상수가 튄다.

### 조치와 그 파급

대칭성 라벨 기반으로 수정 — (xy, yz, zx) 대 (x²−y², 3z²−r²).
연속성은 해결됐고 팔면체 검증(10Dq = 0.7 정확)도 통과.

**그런데 물리가 달라진다:**

| | 부호 기반 (기존) | 대칭성 기반 (수정) | 논문 |
|---|---|---|---|
| D (BFSO) | 1.461 meV | 1.181 meV | **1.45** |
| D (SFSO) | 1.172 meV | 1.134 meV | **1.22** |
| 4°→12° 변화 | +72% | +11% | Fig. 3은 큰 증가 |
| 연속성 | 4.5° 불연속 | 매끄러움 | — |

이유: 대칭성 기반은 t₂–e 분열을 항상 `ten_dq`로 고정하므로 왜곡 효과가 그룹 내부 분열로만 남고
전체 스케일 변화가 지워진다. 부호 기반은 그룹 구성이 바뀌며 스케일이 함께 변했고,
그게 결과적으로 논문의 ΔE 감소 → D 증가 메커니즘을 냈다.

두 정규화의 raw 분열 비율:

| 구조 | 부호 기반 \|dq\| | 대칭성 기반 \|dq\| | 비율 |
|---|---|---|---|
| SFSO (4.86°) | 0.010398 | 0.009192 | 1.13 |
| BFSO (7.84°) | 0.016638 | 0.008146 | **2.04** |

**비율이 구조마다 크게 다르다.** 부호 기반 정규화가 왜곡에 따라 스케일을 크게 바꾸고 있었다는 뜻이고,
논문 D(Δθ) 기울기의 상당 부분이 여기서 왔을 가능성이 있다.

대칭성 기반 raw dq는 왜곡이 커지면 오히려 **감소**한다(0.0092 → 0.0081, −11%).
부호 기반은 **증가**한다(0.0104 → 0.0166, +60%).

### 결정 (사용자)

- **정규화 방식: 기준점 고정 + 점전하 절대변화.**
  기준 구조 한 점에서만 스케일을 맞추고, 다른 각도는 점전하 모델이 주는 절대 변화를 따른다.
  점전하 모델은 리간드 위치가 정해지면 결정장이 절대적으로 결정되므로, 매 각도 재정규화는
  그 변화를 버리고 모양만 남긴다.
- **논문 처리: 방법론 기여로 명시.** 왜곡 스캔에서 정규화 규약이 D(Δθ) 기울기를 좌우한다는 점을 본문/보조자료에 기술.

### 구현

`geometry.py`에 추가:
- `_point_charge_field(positions, r2, r4)` — 정규화 전 점전하 행렬과 Qkm
- `t2_e_splitting(h)` — 대칭성 라벨 기반 t₂–e 분열
- `reference_scale(positions, ten_dq, r2, r4)` — 기준 구조의 보정 계수
- `crystal_field(..., scale=None)` — `scale`을 주면 재정규화 없이 그 보정을 사용

### 다음 검증 (미완)

`ten_dq = 0.30`은 **부호 기반 정규화 하에서 fit된 값**이므로 규약을 바꾸면 다시 맞춰야 한다.
확인할 것:

1. 대칭성 기반 + 기준점 고정에서 BFSO의 D = 1.45 meV를 주는 `ten_dq` 찾기
2. 그 스케일을 고정한 채 SFSO를 계산해 D = 1.22 meV가 나오는지
3. 나오면 새 방식이 논문과 일관되면서 물리적으로도 옳다는 뜻
4. 안 나오면 논문의 왜곡 의존성이 정규화 인공물이라는 뜻 — 그 자체로 중요한 발견이지만
   중성자 실측 D = 1.42 meV와의 정합을 다시 따져야 한다

**주의**: 이 수정은 XAS 스펙트럼 계산에도 영향을 준다.
`examples/` 기준값은 부호 기반으로 생성된 것이므로 재생성 여부를 판단해야 한다.

### 검증 결과 — 해결

`ten_dq`는 관측량이 아니라 **정규화 규약 하에서 fit된 값**이므로, 규약을 바꾸면
측정 가능한 양(D)에 대해 다시 맞춰야 한다. `calibrate_ten_dq()` 추가.

**D(ten_dq)가 비단조**라는 것을 먼저 확인했다:

| ten_dq (eV) | 0.05 | 0.15 | 0.30 | 0.60 | 1.00 | 1.50 | 2.00 |
|---|---|---|---|---|---|---|---|
| D (meV) | 1.837 | 1.452 | 1.181 | 1.060 | 1.469 | 5.575 | 30.54 |

0.6 eV 근처가 최소이고 그 위로 다시 증가한다. 1.5 eV 이상에서는 E₂가 98 meV로 튀는데,
결정장이 너무 커서 **S=2 다중항이 더 이상 최저 5개 상태가 아니게 된** 것이다.
고스핀 d⁶ 사면체에서 물리적으로 유효한 것은 약한 장 가지이므로 브래킷을 (0.05, 0.55)로 제한.

**캘리브레이션 결과 (BFSO D = 1.45 meV 기준)**

```
ten_dq = 0.3000 eV (부호 기반)  →  0.1508 eV (대칭성 기반)
비율 1.99  ≈  앞서 측정한 BFSO raw dq 비율 2.04
```

정확히 예상대로다. 두 규약이 같은 물리를 다른 숫자로 부르고 있었을 뿐이다.

**BFSO 스케일 고정 후 SFSO 예측** (SFSO는 캘리브레이션에 쓰지 않음)

| | 계산 | 논문 | 차이 |
|---|---|---|---|
| D (BFSO) | 1.4500 meV | 1.45 | 캘리브레이션 기준점 |
| **D (SFSO)** | **1.3177 meV** | **1.22** | **+8%** |
| D_BFSO − D_SFSO | 0.132 meV | 0.23 meV | 57% 재현 |

**세 규약 비교**

| 규약 | 4°→12° D 변화 | D_c(1.65) 도달 | 연속성 |
|---|---|---|---|
| 부호 기반 (논문 계산) | +72% | 9.63° | ✗ 4.5° 불연속 |
| 대칭성 기반, 매 각도 재정규화 | +11% | 도달 못함 | ✓ |
| **대칭성 기반 + 기준점 고정 (채택)** | **+33%** | **11.09°** | ✓ |

### 해석

- 논문의 정성적 결론은 **유지된다**: 왜곡이 커지면 D가 증가하고, 10~11° 부근에서 임계값에 도달한다.
- 다만 왜곡 의존성의 세기가 부호 기반보다 **약하다**(+72% → +33%). 부호 기반 기울기의 절반 남짓은
  정규화 규약이 만든 인공물이었다고 보는 것이 타당하다.
- SFSO를 8% 이내로 예측한 것은, BFSO 한 점만 보정하고 나머지는 점전하 모델에 맡긴 것치고 양호하다.
- D_c 도달 각도가 9.63° → 11.09°로 밀렸다. **변형 제어로 임계에 도달하기가 논문 예측보다 어렵다**는
  뜻이므로, 실험 설계에 직접 영향을 주는 수정이다.

### 규약 변경이 물리를 바꾸지 않음을 증명

두 규약이 같은 해밀토니안을 주는 `ten_dq` 값을 계산:

```
BFSO에서  old scale = 0.30 / |dq_sign|  = 18.031
          등가 ten_dq = 18.031 x |dq_symm| = 0.146884 eV
```

이 값으로 XAS를 재계산해 이전 기준값과 비교:

| iso | z | ld | ab | x/y |
|---|---|---|---|---|
| 7.4e-8 | 2.4e-7 | 2.5e-7 | 1.0e-7 | 4.2e-6 |

(x/y의 4e-6은 기존부터 있던 축퇴 기저 노이즈)

**규약 변경은 물리를 바꾸지 않는다. `ten_dq` 값의 재정의일 뿐이다.**
따라서 문헌에서 10Dq 값을 인용할 때는 어느 규약인지 함께 밝혀야 한다.

### 원조 XTLS와의 대조 — 별도 문제 발견

`data/Fe_L_spectrum.txt`(원조 XTLS 결과)와 우리 계산을 비교하니 **에너지 축이 약 5 eV 어긋나** 있었다.

- 원조 XTLS `iso_broadened` 주 피크: −2.92 eV
- 우리 `iso` 주 피크: +2.05 eV

입력 파일의 `xtls_energy_shift = 0.0`이 잘못된 값이었고, README가 말하는
"원조 XTLS 결과를 겹쳐서 검증"이 실제로는 어긋난 채 그려지고 있었다.

최적 정렬(−4.93 eV) 후 잔차:

| 대조 대상 | 최적 shift | RMS |
|---|---|---|
| `iso_broadened` (브로드닝 후, 올바른 대조군) | −4.93 eV | **5.8%** |
| `iso` (브로드닝 전) | −4.97 eV | 20.5% |

**우리 파이썬 구현이 원조 XTLS를 5.8% RMS로 재현한다.** 두 코드의 에너지 영점이 다를 뿐이다.
`ten_dq` 0.146884와 0.150800의 재현도 차이는 없다(5.809% vs 5.820%).

### 조치

`inputs/Fe_Ba2FeSi2O7.py` 갱신:
- `ten_dq = 0.30` → **0.1508** (대칭성 규약, D_BFSO = 1.45 meV 재현). 규약 주석 추가.
- `xtls_energy_shift = 0.0` → **−4.93** (오버레이 정렬)
- `examples/` 결과 재생성

---

## 3단계: Poisson 비 감도 — 완료

논문 빈틈 ③에 대한 답. 논문은 ν = 0.492를 4°~12° 전 구간에 고정 외삽했고,
그 가정이 결론에 얼마나 영향을 주는지는 다루지 않았다.

`bond_length_for(angle, slope_factor)`로 dR/dΔθ를 배수 조정해 스캔.
측정된 기울기는 **0.009162 Å/deg** (두 실험 구조에서).

| 배수 | R(12°) | D(4°) | D(12°) | 변화 | D_c 도달 |
|---|---|---|---|---|---|
| 0.0 (거리 고정) | 1.99626 | 1.362 | 1.636 | +20.1% | **도달 못함** |
| 0.5 | 2.01534 | 1.324 | 1.676 | +26.6% | 11.61° |
| **1.0 (실험값)** | 2.03442 | 1.287 | 1.718 | **+33.5%** | **11.09°** |
| 1.5 | 2.05350 | 1.252 | 1.760 | +40.6% | 10.68° |
| 2.0 | 2.07257 | 1.219 | 1.804 | +48.0% | 10.35° |

### 해석

1. **각도 변화 자체가 주된 기여다.** 결합거리를 완전히 고정해도 D가 20% 증가한다.
   즉 논문의 물리적 메커니즘(왜곡 → ΔE 감소 → D 증가)은 Poisson 가정에 의존하지 않는다.
2. **다만 임계 도달 여부는 갈린다.** 거리 고정 시 12°까지 가도 D_c에 못 미친다.
   결합거리 신축이 임계 도달에 필수적이라는 뜻.
3. **도달 각도는 10.35°~11.61° 범위.** Poisson 가정을 2배까지 흔들어도 ±0.6° 정도다.
   논문의 "≈10°"보다 다소 크지만 같은 자릿수.

→ **DFT 구조 이완(6단계)의 동기가 명확해졌다.** 지금은 dR/dΔθ가 두 화합물의
화학적 압력에서 온 경험값인데, 실제 변형 하에서의 이완을 DFT로 구하면 이 ±0.6°를 좁힐 수 있다.

---

## 현재까지 확보한 후속 논문 재료

| | 내용 | 상태 |
|---|---|---|
| A | 결정장 정규화 규약이 D(Δθ) 기울기를 좌우한다는 방법론적 발견 | ✓ |
| B | 규약 무관하게 D(BFSO)=1.45를 재현, SFSO는 8% 이내 예측 | ✓ |
| C | D_c 도달 각도 수정: 9.6° → **11.1°** (변형 제어가 더 어려움) | ✓ |
| D | Poisson 가정 감도 정량화: 10.35°~11.61° | ✓ |
| E | 4차 이방성 A와 Sz=±2 터널링 분열 (논문에 없음) | ✓ |
| F | 우리 구현이 원조 XTLS를 5.8% RMS로 재현 | ✓ |
| G | A₂MX₂O₇ 계열 스크리닝 | 미착수 |
| H | XPS·XMCD·RIXS 예측 | XPS만 가능 |
| I | DFT 구조 이완 + J(Δθ) | 환경 없음 |

### 남은 확인

- [ ] SFSO 8% 차이의 원인. 논문이 SFSO에 별도 조정을 했는지, 점전하 모델의 한계인지.
- [ ] 4차 이방성 A의 부호가 구조에 따라 바뀌는 이유 (BFSO +0.0017, SFSO +0.0002).
- [ ] Sz=±2 터널링 분열의 대칭성 기원. S₄ 대칭 하에서 허용되는 항인지 확인 필요.
